model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review like an owner. Prioritize correctness, security, regressions, and
missing tests. Lead with concrete findings and avoid style-only feedback.
"""
